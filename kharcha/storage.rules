rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Attachments are strictly private to the authenticated owner.
    match /users/{userId}/attachments/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
